// HTML
-> hyper text markup language
-> which help us to create the base of website 
-> it only make ui
-> HTMl is not case sensitive <h1> === <H1> -> we can write in capital and small case, and it will not effects on process 
-> you can view your output at any browser (chrome/firefox/brave)

// Tag
-> Tag is a represent how ui look
-> like if we write h1 tag so that text will looks big then h6 tag

None self closing
<p> -> opening tag
</p> -< closing tag

<hr/> -> self closing tag


// attribute 
-> extra style (limited style)
<Table border="" ></Table>


// design type

Symentric => use proper tag, for example for contect use p tag, heading h1 tag.
non symentrict => do not use proper tag, for example use p tag for contecnt and heading both.

-> how to show 1.2.3. in list - 
-> show content as user write in ui - done
-> only allow png/jpg image - done
-> what is difference between radio and checkbox