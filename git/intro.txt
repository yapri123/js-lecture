-> Git is a version control manager

-> create Git account
-> create reposetory (public)
-> clone the created reposirtoy
    - git clone <URL>
-> to uplod a code 
    - add the file which you want to commit - git add . -> all file 
    - commit the code - git commit -m "relevent msg"
    - push the code  - git push
